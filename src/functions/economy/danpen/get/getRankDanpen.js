/** 
 * 
 * retorna ranking de danpen
 * @param {any} guildID - ID do servidor (opcional)
 * 
 * **/
export async function getRankDanpen(options = { guildID: 0 }) {

    if (!options.guildID || options.guildID === 0) {
        //code ranking global
    } else {
        // code ranking guild
    }

}