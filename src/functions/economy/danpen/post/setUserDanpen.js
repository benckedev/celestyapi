/**
* função para setar danpens do usuário
* @param {any} id - id do usuário
* @param {number} amount - quantia para setar
**/
export async function setUserDanpen(id, amount) {
    // func setar danpen na db
    return amount;
}