/**
* função para adicionar danpen ao usuário
* @param {any} id - id do usuário
* @param {number} amount - quantia para adicionar
**/
export async function addUserDanpen(id, amount) {
    // func add danpen da db
    return amount;
}